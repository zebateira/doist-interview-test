import styled from 'styled-components';

import { contentSpace } from '../../styles/layout';

export const Container = styled.div`
    ${contentSpace};
    padding-top: 40px;
    padding-bottom: 40px;
`;
